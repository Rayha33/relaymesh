services:
  relaymesh:
    build: .
    ports:
      - "127.0.0.1:4317:4317"
    volumes:
      - relaymesh-data:/app/data
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

volumes:
  relaymesh-data:
